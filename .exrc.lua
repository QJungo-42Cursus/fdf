print('salut')

require('telescope').setup{ defaults = { file_ignore_patterns = { "test_maps", "%.o", "minilibx*" } }}
--require('telescope').setup{ defaults = { file_ignore_patterns = { "libft", "test_maps", "%.o", "minilibx*" } }}
